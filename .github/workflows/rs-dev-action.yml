name: Rust

on:
  push:
    branches: [ rs-dev ]
  pull_request:
    branches: [ rs-dev ]

jobs:
  build:

    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
